export type UserFormData = {
  email: string;
  password: string;
};

export type UserData = {
  isAuth: boolean;
  email: string;
  token: string;
  uid: string;
};

export type UserDataType = {
  userId: string;
  name: string;
  surname: string;
};
